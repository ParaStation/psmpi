# This Makefile.in created by simplemake.  Do not edit

# @configure_input@

# --------------------------------------------------------------------------
all: all-redirect

SHELL           = @SHELL@
AR              = @AR@
RANLIB          = @RANLIB@
master_top_srcdir  = @master_top_srcdir@
top_builddir    = @master_top_builddir@
abs_builddir    = @abs_builddir@
srcdir          = @srcdir@
abs_srcdir      = @abs_srcdir@
top_srcdir      = @top_srcdir@
DEFS            = @DEFS@ -I. -I${srcdir}
CPPFLAGS        = @CPPFLAGS@
LIBS            = @LIBS@
MAKE            = @MAKE@
MPILIBNAME      = @MPILIBNAME@
PMPILIBNAME     = @PMPILIBNAME@

CC              = @CC@
CFLAGS          = @CFLAGS@ $(MPICH2_MAKE_CFLAGS)
C_COMPILE       = $(CC) $(DEFS) $(INCLUDES) $(CFLAGS) $(CPPFLAGS)
C_LINK         = $(CC)
LIBTOOL         = @LIBTOOL@
CC_SHL          = @CC_SHL@
C_COMPILE_SHL   = $(CC_SHL) $(DEFS) $(INCLUDES) $(CFLAGS) $(CPPFLAGS)
LDFLAGS     =  @LDFLAGS@ 

VPATH = .:@srcdir@
HEADERS = trmem.h
INCLUDES = -I../../include -I${top_srcdir}/src/include

.SUFFIXES:
.SUFFIXES: .o .lo .c .txt 
.c.o:
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "  CC              $<" ; \
	else \
	  echo $(C_COMPILE) -c $< ; \
	fi
	@$(C_COMPILE) -c $<
.c.lo:
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "  CC              $<" ; \
	else \
	  echo $(C_COMPILE_SHL) -c $< -o _s$*.o ; \
	echo mv -f _s$*.o $*.lo ; \
	fi
	@$(C_COMPILE_SHL) -c $< -o _s$*.o
	@mv -f _s$*.o $*.lo
.c.txt:
	$(CPP) $(INCLUDES) $(CPPFLAGS) $< >$*.txt
.c:
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "  CC              $<" ; \
	else \
	  echo $(C_COMPILE) -o $* $< $(LDFLAGS) $(LIBS) ; \
	fi
	@$(C_COMPILE) -o $* $< $(LDFLAGS) $(LIBS)

# --------------------------------------------------------------------------
${srcdir}/Makefile.in: ${srcdir}/Makefile.sm
	( cd ${srcdir} && ${master_top_srcdir}/maint/simplemake \
	-rootdir=../../../ \
	-configdir=. \
	-common=${master_top_srcdir}/maint/makedefs \
	-docnamedefs=${master_top_srcdir}/maint/docnotes \
	"-autoconf=-I ROOTDIR/confdb" \
	-libdir='$${MPILIBNAME}=ROOTDIR/lib' \
	-smroot=${master_top_srcdir}/maint \
	-distcleanfiles="createshlib Makefile" \
	-smvar_doc_attop=0 \
		Makefile.sm )
Makefile: ${srcdir}/Makefile.in
	-cd ../../../ && \
	if [ -x config.status ] ; then CONFIG_FILES=src/util/mem/Makefile CONFIG_HEADERS= ${SHELL} ./config.status ; fi
# --------------------------------------------------------------------------
apply: 
	$(ACTION) $(ACTION_INPUT)
clean: 
	-@rm -f *.o ${srcdir}/*.o check
	-@rm -f .libstamp0 .libstamp1 
	-@rm -f check.exe
	-@rm -f core core.[0-9]*
	-@rm -f *.lo ${srcdir}/*.lo
	-@rm -f ${srcdir}/*.bb ${srcdir}/*.bbg ${srcdir}/*.da
	-@rm -f ${srcdir}/*.gcda ${srcdir}/*.gcno
	-@rm -f *.gcov *.bb *.bbg *.da *.gcda *.gcno
	-@rm -f *.irlog*
distclean:  clean distclean-xxx remove-makefile
distclean-xxx:
	-@rm -f Makefile
	-@rm -rf autom4te*.cache
	-@rm -rf $(DEPS_DIR)
	-@rm -f TAGS
maintainer-clean:  distclean-xxx remove-genmakefiles
remove-makefile:
	rm -f Makefile
remove-genmakefiles:
	rm -f  Makefile.in Makefile
all-redirect: .libstamp0 ../../../lib/lib${MPILIBNAME}.a
	@if [ "@ENABLE_SHLIB@" != "none" ] ; then \
	    ${MAKE} .libstamp1 || exit 1; \
	fi

.libstamp0 ../../../lib/lib${MPILIBNAME}.a: trmem.o handlemem.o safestr.o \
	argstr.o 
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "  AR cr ../../../lib/lib${MPILIBNAME}.a $?" ; \
	else \
	  echo ${AR} cr ../../../lib/lib${MPILIBNAME}.a $? ; \
	fi
	@${AR} cr ../../../lib/lib${MPILIBNAME}.a $?
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "  RANLIB          lib${MPILIBNAME}.a" ; \
	else \
	  echo ${RANLIB} ../../../lib/lib${MPILIBNAME}.a ; \
	fi
	@${RANLIB} ../../../lib/lib${MPILIBNAME}.a
	date > .libstamp0
.libstamp1 ../../../lib/lib${MPILIBNAME}.la: trmem.lo handlemem.lo safestr.lo \
	argstr.lo 
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "  AR cr ../../../lib/lib${MPILIBNAME}.la $?" ; \
	else \
	  echo ${AR} cr ../../../lib/lib${MPILIBNAME}.la $? ; \
	fi
	@${AR} cr ../../../lib/lib${MPILIBNAME}.la $?
	date > .libstamp1
coverage: 
	-@for file in trmem.c handlemem.c safestr.c argstr.c ; do \
		gcov -b -f $$file ; done
check: safestr.o check.o   
	$(C_LINK) $(CFLAGS) $(LDFLAGS) -o check safestr.o check.o  ${LIBS}
# --------------------------------------------------------------------------
htmldoc:
mandoc:
latexdoc:
# --------------------------------------------------------------------------
# --------------------------------------------------------------------------
# --------------------------------------------------------------------------
# --------------------------------------------------------------------------
# Dependencies
DEPS_DIR = .deps
MAKE_DEPEND_C   = @MAKE_DEPEND_C@ $(DEFS) $(INCLUDES) $(CPPFLAGS)

dependencies: $(DEPS_DIR)/timestamp
$(DEPS_DIR)/trmem.d: $(srcdir)/trmem.c
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "MAKE_DEPEND_C trmem.c > $(DEPS_DIR)/trmem.d" ; \
	else \
	  echo $(MAKE_DEPEND_C)  $(srcdir)/trmem.c >$(DEPS_DIR)/trmem.d ; \
	fi
	@$(MAKE_DEPEND_C)  $(srcdir)/trmem.c >$(DEPS_DIR)/trmem.d
$(DEPS_DIR)/handlemem.d: $(srcdir)/handlemem.c
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "MAKE_DEPEND_C handlemem.c > $(DEPS_DIR)/handlemem.d" ; \
	else \
	  echo $(MAKE_DEPEND_C)  $(srcdir)/handlemem.c >$(DEPS_DIR)/handlemem.d ; \
	fi
	@$(MAKE_DEPEND_C)  $(srcdir)/handlemem.c >$(DEPS_DIR)/handlemem.d
$(DEPS_DIR)/safestr.d: $(srcdir)/safestr.c
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "MAKE_DEPEND_C safestr.c > $(DEPS_DIR)/safestr.d" ; \
	else \
	  echo $(MAKE_DEPEND_C)  $(srcdir)/safestr.c >$(DEPS_DIR)/safestr.d ; \
	fi
	@$(MAKE_DEPEND_C)  $(srcdir)/safestr.c >$(DEPS_DIR)/safestr.d
$(DEPS_DIR)/argstr.d: $(srcdir)/argstr.c
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "MAKE_DEPEND_C argstr.c > $(DEPS_DIR)/argstr.d" ; \
	else \
	  echo $(MAKE_DEPEND_C)  $(srcdir)/argstr.c >$(DEPS_DIR)/argstr.d ; \
	fi
	@$(MAKE_DEPEND_C)  $(srcdir)/argstr.c >$(DEPS_DIR)/argstr.d
$(DEPS_DIR)/timestamp: trmem.c handlemem.c safestr.c argstr.c safestr.c check.c Makefile
	rm -f $(DEPS_DIR)/newalldeps
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "MAKE_DEPEND_C trmem.c > $(DEPS_DIR)/trmem.d" ; \
	else \
	  echo $(MAKE_DEPEND_C)  $(srcdir)/trmem.c >$(DEPS_DIR)/trmem.d ; \
	fi
	@$(MAKE_DEPEND_C)  $(srcdir)/trmem.c >$(DEPS_DIR)/trmem.d
	echo "include $(DEPS_DIR)/trmem.d" >>$(DEPS_DIR)/newalldeps
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "MAKE_DEPEND_C handlemem.c > $(DEPS_DIR)/handlemem.d" ; \
	else \
	  echo $(MAKE_DEPEND_C)  $(srcdir)/handlemem.c >$(DEPS_DIR)/handlemem.d ; \
	fi
	@$(MAKE_DEPEND_C)  $(srcdir)/handlemem.c >$(DEPS_DIR)/handlemem.d
	echo "include $(DEPS_DIR)/handlemem.d" >>$(DEPS_DIR)/newalldeps
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "MAKE_DEPEND_C safestr.c > $(DEPS_DIR)/safestr.d" ; \
	else \
	  echo $(MAKE_DEPEND_C)  $(srcdir)/safestr.c >$(DEPS_DIR)/safestr.d ; \
	fi
	@$(MAKE_DEPEND_C)  $(srcdir)/safestr.c >$(DEPS_DIR)/safestr.d
	echo "include $(DEPS_DIR)/safestr.d" >>$(DEPS_DIR)/newalldeps
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "MAKE_DEPEND_C argstr.c > $(DEPS_DIR)/argstr.d" ; \
	else \
	  echo $(MAKE_DEPEND_C)  $(srcdir)/argstr.c >$(DEPS_DIR)/argstr.d ; \
	fi
	@$(MAKE_DEPEND_C)  $(srcdir)/argstr.c >$(DEPS_DIR)/argstr.d
	echo "include $(DEPS_DIR)/argstr.d" >>$(DEPS_DIR)/newalldeps
	@if [ "x$(VERBOSE)" != "x1" -a "x$(V)" != "x1" ] ; then \
	  echo "MAKE_DEPEND_C check.c > $(DEPS_DIR)/check.d" ; \
	else \
	  echo $(MAKE_DEPEND_C)  $(srcdir)/check.c >$(DEPS_DIR)/check.d ; \
	fi
	@$(MAKE_DEPEND_C)  $(srcdir)/check.c >$(DEPS_DIR)/check.d
	echo "include $(DEPS_DIR)/check.d" >>$(DEPS_DIR)/newalldeps
	if [ -s $(DEPS_DIR)/newalldeps ] ; then mv -f $(DEPS_DIR)/newalldeps $(DEPS_DIR)/alldeps ; fi
	date >$(DEPS_DIR)/timestamp
include $(DEPS_DIR)/alldeps
# End of Dependencies
# --------------------------------------------------------------------------
# --------------------------------------------------------------------------
SOURCES         =  trmem.c handlemem.c safestr.c argstr.c safestr.c check.c
HEADERFILES     = $(HEADERS)
SOURCEFILES     = $(SOURCES)
# --------------------------------------------------------------------------

# check is a program to test MPIU_Snprintf

FORCE_TARGET:

